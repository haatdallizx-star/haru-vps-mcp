"""Console entry point placeholder for the HealthKit ingest service.

The ASGI application is implemented in a later task. Keeping this module importable
allows editable installs and deployment-example validation to resolve the console
script during the settings task.
"""


def main() -> int:
    raise RuntimeError("HealthKit ingest ASGI service is not implemented yet")
